declare module 'index' {
    global {
        const tree: Branch[]
        const closeSpan: HTMLElement
        const aboutWindow: HTMLElement
        const openAbout: HTMLElement

        let angle: number
        let slider: any
        let root: Branch
        let inp: any
    }
}